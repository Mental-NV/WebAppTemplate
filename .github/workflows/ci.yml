name: CI

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

defaults:
  run:
    shell: pwsh

jobs:
  ralph-smoke:
    name: Ralph Smoke (Backlog CLI)
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          global-json-file: global.json

      - name: Validate Ralph backlog file
        run: ./scripts/ralph/backlog.ps1 validate

      - name: Run Ralph Backlog CLI smoke test
        run: ./scripts/ralph/smoke-backlog-cli.ps1

  ci:
    name: CI (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    env:
      Google__ClientId: ci-test.apps.googleusercontent.com
      Jwt__SigningKey: ci_test_signing_key_32_chars_minimum_123456
      VITE_GOOGLE_CLIENT_ID: ci-test.apps.googleusercontent.com
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Temporary Windows CI failure (task-303)
        if: github.event_name == 'pull_request' && github.head_ref == 'ralph/task-303' && runner.os == 'Windows'
        run: |
          Write-Host 'Intentional task-303 Windows CI failure for first PR attempt.'
          exit 1

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          global-json-file: global.json

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: src/web/package-lock.json

      - name: Build
        run: ./scripts/build.ps1 -Configuration Release -CI

      - name: CI checks
        run: ./scripts/ci.ps1 -Configuration Release -CI

      - name: Publish
        run: ./scripts/publish.ps1 -Configuration Release -CI

      - name: Install Playwright browsers (Linux)
        if: runner.os == 'Linux'
        working-directory: src/web
        run: npx playwright install --with-deps chromium

      - name: Install Playwright browsers (Windows)
        if: runner.os == 'Windows'
        working-directory: src/web
        run: npx playwright install chromium

      - name: Run Playwright E2E
        env:
          Jwt__SigningKey: ${{ secrets.JWT__SIGNINGKEY }}
          HTTPS_CERT_PFX_BASE64: ${{ secrets.HTTPS_CERT_PFX_BASE64 }}
          HTTPS_CERT_PASSWORD: ${{ secrets.HTTPS_CERT_PASSWORD }}
          E2E_AUTH_SECRET: ci-e2e-secret
        working-directory: src/web
        run: npx playwright test --project=chromium
